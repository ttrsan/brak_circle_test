変更内容:
- スマホタブを height 固定 + flex中央寄せに変更
- style.css / script.js を読みやすい改行付きに整理


変更内容:
- 設定タブから「設定を反映」ボタンを削除
- 送信者名を入力した時点で即時反映
- 伏字モードも選択時点で即時反映
\n変更内容:\n- NGワード判定でひらがな/カタカナを同一視\n- 例: 「しね」登録時、「シネ」もNG判定\n
変更内容:
- 全角半角英字を同一視
- 大文字小文字を同一視
- ひらがなカタカナを同一視
- allowWords を追加
- 半角カナ部分のみNG処理


変更内容:
- 設定タブに「許容ワード一覧」ボタンを追加
- 「NGワード一覧を表示」を「NGワード一覧」に変更
- 許容ワード一覧をモーダル表示


変更内容:
- ngWords / allowWords を word-settings.js へ分離
- script.js を処理専用へ整理
- index.html に word-settings.js を追加


コメント:
- 差別語・歴史的蔑称系がかなり増えてきたため、
  将来的にはカテゴリ別ファイル分離がおすすめです。
- 例:
  ngwords-adult.js
  ngwords-violence.js
  ngwords-slur.js


変更内容:
- 新しいチャットを上に表示するよう変更
- appendChild → prepend へ変更


変更内容:
- 新しいチャットを下表示へ戻した
- 入力欄下 + 新着下 の一般的なチャットUIへ戻した


変更内容:
- chatLog と chatForm のDOM順を変更
- チャットログを上、入力欄を下へ配置


変更内容:
- テストサークル右側にメニューボタンを追加
- 説明文を常時表示からモーダル表示へ変更
- サイト情報モーダルに制作表記を移動


変更内容:
- 入力欄を上へ戻した
- 最新チャットを上表示へ変更
- 「入力→即確認」向けUIへ変更
