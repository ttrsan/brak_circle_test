変更内容:
- スマホタブを height 固定 + flex中央寄せに変更
- style.css / script.js を読みやすい改行付きに整理


変更内容:
- 設定タブから「設定を反映」ボタンを削除
- 送信者名を入力した時点で即時反映
- 伏字モードも選択時点で即時反映
\n変更内容:\n- NGワード判定でひらがな/カタカナを同一視\n- 例: 「しね」登録時、「シネ」もNG判定\n
変更内容:
- 全角半角英字を同一視
- 大文字小文字を同一視
- ひらがなカタカナを同一視
- allowWords を追加
- 半角カナ部分のみNG処理
